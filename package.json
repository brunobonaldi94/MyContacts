{
  "name": "mycontacts_final",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install": "yarn --cwd server install && yarn --cwd fe install",
    "start": "docker start pg && yarn --cwd server dev && yarn --cwd fe start"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eslint-plugin-react-hooks": "^4.6.0"
  }
}
